<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario de Producto</title>
    <link rel="stylesheet" href="/css/agregar.css">
</head>
<body>

    <header onclick="window.location.href='/'">
        <h1 class="h1Header">Santa Rita </h1>
    </header>
    <section>

        <h2>Editar Producto</h1>
    </section>

    <form action="/editar/<%= producto.id %>" method="POST" class="contForm">
        <div>
            <label for="producto">Nombre del Producto:</label>
            <input type="text" id="producto" name="nombre" required value="<%= producto.nombre %>">
        </div>
        
        <div>
            <label for="precio">Precio:</label>
            <input type="number" id="precio" name="precio" step="0.01" required  value="<%= producto.precio %>">
        </div>
        
        <div>
            <label for="stock">Stock:</label>
            <input type="number" id="stock" name="stock" min="0" required  value="<%= producto.stock %>">
        </div>
    
        
    
        <button type="submit">Enviar</button>
    </form>
    
    
<script src="js/agregar.js"></script>
</body>
</html>
